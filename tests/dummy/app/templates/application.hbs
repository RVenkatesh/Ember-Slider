<h2 id="title">Ember slider demo</h2>
This page provides an overview of the basic slider options available.
<div class="demo basic-slider">
    <h3>Basic 0 to 100 slider</h3>
    <div class="code">
        <h5>Code</h5>
        {{code-snippet name="basic-slider-options.js"}}
        {{code-snippet name="basic-slider-options.hbs"}}
    </div>
    <div class="preview">
        <h5>Preview</h5>
        <p>This is a basic slider setup. Try clicking on the below buttons to toggle value display and change the look of the slider</p>
        <div class="slider-container">
            {{ember-slider config=options}}
        </div>
        <div 
          class="demo-button"
          onmousedown={{action "addActiveClass"}}
          onmouseup={{action "removeActiveClass"}}
          {{action "toggleValueDisplay" options}}
        >
            Toggle value display
        </div>
        <div 
          class="demo-button"
          onmousedown={{action "addActiveClass"}}
          onmouseup={{action "removeActiveClass"}}
          {{action "toggleSliderType" options}}
        >
            Toggle slider type
        </div>
    </div>
</div>
<div class="demo likert-slider">
    <h3>5 point likert slider</h3>
    <div class="code">
        <h5>Code</h5>
        {{code-snippet name="likert-slider-options.js"}}
        {{code-snippet name="basic-slider-options.hbs"}}
    </div>
    <div class="preview">
        <h5>Preview</h5>
        <p>This is a 5 point scale likert slider setup. Try clicking on the below buttons to toggle value display and change the look of the slider</p>
        <div class="slider-container">
            {{ember-slider config=likertOptions}}
        </div>
        <div 
          class="demo-button"
          onmousedown={{action "addActiveClass"}}
          onmouseup={{action "removeActiveClass"}}
          {{action "toggleValueDisplay" likertOptions}}
        >
            Toggle value display
        </div>
        <div 
          class="demo-button"
          onmousedown={{action "addActiveClass"}}
          onmouseup={{action "removeActiveClass"}}
          {{action "toggleSliderType" likertOptions}}
        >
            Toggle slider type
        </div>
    </div>
</div>
{{outlet}}
